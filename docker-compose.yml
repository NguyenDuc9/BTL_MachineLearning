services:
  app:
    build: .
    env_file:
      - path: .env
        required: false
    ports:
      - '${APP_PORT:-8000}:${APP_PORT:-8000}'
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    command: uvicorn app.main:app --host ${APP_HOST:-0.0.0.0} --port ${APP_PORT:-8000}
